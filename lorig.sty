% This file defines the "lorig" latex package.
%
% Written by 
%	Matt Lorig,    matt.lorig@gmail.com
%	Weston Barger, weston.barger@gmail.com
% 
% Page geometry is
%	paperwidth		=8.5in
%   paperheight		=11in
% while margins are
%	top				=1.25in 
%	bottom			=1.25in
%	left			=1.0in 
%	right			=1.0in
%
% Optional arguments:
%
%	Mutually exclusive argments:
%		frenchmath OR italics   (default: frenchmath) -  defines option to be used for mathastext package
%		toDVItoPDf OR toPStoPDF (default: toPStoPDF)  -  the toDVItoPDf specifices \RequirePackage[pdftex]{graphicx}\RequirePackage{epstopdf}
%														 while toPStoPDF specifices  \RequirePackage{graphicx}
%   Other options:
%		draft       - when specified, equation labels are shown on the left
%		doublespace - when specified, document is doublespaced
%
%
% 


\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{lorig}[2017/05/01 Lorig Package, written by Matt Lorig and Weston Barger]




%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% 				OPTION DECLARATION
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% options for mathastext
% these options are mutually exclusive
\newcounter{mathastextcounter}
\setcounter{mathastextcounter}{-1}
\newif\if@it
\newif\if@fm
\DeclareOption{italics}{
	\stepcounter{mathastextcounter}
	\@fmfalse
	\@ittrue
}
\DeclareOption{frenchmath}{
	\stepcounter{mathastextcounter}
	\@fmtrue
	\@itfalse
}


% use matt's macros?
\newif\if@lmacro
\@lmacrofalse
\DeclareOption{lmacro}{
	\@lmacrotrue
}



% draft
% i.e. show eqrefs or not
\newif\if@draft
\@draftfalse
\DeclareOption{draft}{
	\@drafttrue
}

\newif\if@doublespace
\@doublespacefalse
\DeclareOption{doublespace}{
	\@doublespacetrue
}

% make the default math script
% frenchmath i.e. no italics on capital letters
\ExecuteOptions{frenchmath}
\ProcessOptions\relax

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% 				REQUIRED PACKAGES
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%





% packages required for 

\RequirePackage[
	paperwidth		=8.5in,
	paperheight		=11in,
	top				=1.25in, 
	bottom			=1.25in, 
	left			=1.0in, 
	right			=1.0in
	]{geometry}



%mathastext tells you to do so? 
\AtBeginDocument{
	\everymath={}
	\everydisplay={}
}

\RequirePackage[T1]{fontenc}
\RequirePackage{lmodern}
\RequirePackage{microtype}


% sets default text font to concrete modern
\renewcommand{\rmdefault}{ccr}
\renewcommand*\familydefault{\rmdefault}

% ams and other math
\RequirePackage{amssymb}
\RequirePackage{amsmath}
\RequirePackage{amsthm}

% gives you \mathscr font
\RequirePackage[mathscr]{eucal}
% gives you \mathds font
\RequirePackage{dsfont}


\RequirePackage{natbib}
\RequirePackage[shortlabels]{enumitem}
\RequirePackage{mdwlist}

% gives colors for hyperref
\RequirePackage[dvipsnames]{xcolor}	

% enables and customizes hyperlinks
\RequirePackage[plainpages=false, pdfpagelabels]{hyperref}

% used for show only labels
\RequirePackage{mathtools}                     

% needed to test for ifpdf
\RequirePackage{ifpdf}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% 				OPTIONAL PACKAGE PROCESSING
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% set the math text style
\ifnum1<\value{mathastextcounter}
	\PackageError{lorig}{Options 'frenchmath' and 'italics' are mutually exclusive.}{}
\fi
\if@fm
	\RequirePackage[frenchmath]{mathastext}
\fi
\if@it
	\RequirePackage[italic]{mathastext}
\fi

% Are we using pdflatex ? 
\ifpdf
	\RequirePackage[pdftex]{graphicx}
	\RequirePackage{epstopdf}
\else
	\RequirePackage{graphicx}
\fi


\if@doublespace
	\linespread{1.6}
\else
	\linespread{1.3}
\fi

\if@lmacro
	\RequirePackage{lmacros}
\fi

\if@draft
	\RequirePackage[left]{showlabels} 
\fi

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% 				STYLE PREFERENCES
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Make \eqnarray illegal.
\renewenvironment{eqnarray}{\PackageError{lorig}{eqnarray is illegal. Use align.}{}}{}
\renewenvironment{eqnarray*}{\PackageError{lorig}{eqnarray is illegal. Use align.}{}}{}


% show only labels
\mathtoolsset{showonlyrefs=true}

% set up the hyperlink style
\hypersetup{
	colorlinks   = true,
	citecolor    = RoyalBlue, %NavyBlue,
	linkcolor    = RubineRed, %Maroon,
	urlcolor     = Turquoise
	}

% allows page breaks for long equations...you can prevent a page-break with \\*
\allowdisplaybreaks               

\numberwithin{equation}{section}

% Theorem style information
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}
\numberwithin{theorem}{section}
% [theorem] ==> theorems and lemmas will share a counter
\newtheorem{lemma}[theorem]{Lemma}       	
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{assumption}[theorem]{Assumption}






\endinput
